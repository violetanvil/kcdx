# Outstanding work

Designed-but-not-built items, each with a named revisit trigger. Counterpart to `docs/known-issues/` (open bugs with diagnostic trails).

## Contract for entries

Each file holds exactly:

1. **Status** — one line: state + what's blocking ship.
2. **Trigger to revisit** — concrete condition under which to pick this up.
3. **Design** — the settled decisions (schema, contract, mechanism).
4. **Files that need to change** — when the trigger fires, the working agent has the change set ready.

No history. No "why we deferred this v3 of the discussion". Pointer to deeper docs (`design-gaps.md`, recon dossiers) at the bottom if needed.

## Status ledger — the canonical "I completed this" surface

A multi-step entry (a phased plan, an item that lands across several commits) carries ONE status ledger and no other status surface. It is the single source of truth for step completion — not prose paragraphs, not inline `— DONE` header tags, not the commit log alone.

Fixed shape, exactly these columns:

| Step | Status | Commit |
|---|---|---|
| `<step id — short label>` | `<NOT STARTED \| IN PROGRESS \| DONE \| BLOCKED \| SUPERSEDED>` | `<(landed) until backfilled, then the short hash; — when not DONE>` |

Rules:
- **One ledger per doc.** A doc with a ledger does NOT also carry a status-prose paragraph or per-header `— DONE` tags — those drift against the table. The ledger wins.
- **The orchestrator writes the row, not a human.** A landed step's row flips to `DONE` in the SAME commit as the step's diff. This is a gated step of the orchestrator cycle (`../../.claude/skills/_shared/orchestrator-loop.md` §C), not a follow-up edit.
- **The hash backfills one commit later — a commit cannot contain its own hash.** The step's own commit lands `DONE` + `(landed)` in the Commit cell (NOT a hash — the landing commit's hash does not exist yet when that commit is built). The real short hash is filled by the NEXT ledger-touching commit: the next step flips its own row AND replaces the prior row's `(landed)` with the now-known hash. The final step of a plan leaves a trailing `(landed)`; a tiny follow-up commit backfills it. Never write a hash into the commit that the hash names — that yields the PRIOR commit's hash (the stale-hash bug).
- **Status vocabulary is closed.** `BLOCKED`/`SUPERSEDED` rows state the blocker/successor in the step label (e.g. `11b force-load — blocked on 11a`), not in an extra column.
- **The active "go-to" plan doc is born with a ledger.** Whatever doc currently drives sequenced work carries this table from creation; its retirement does not retire the convention — the next active plan inherits it. A multi-phase plan tree (top README + `phase-NN-<slug>/` subdirs + per-step docs + `context.md`) is authored by `/plan` from a settled goal; the executing cycle (`/feature` / `/execute`) reads a step doc as its `Source work-item` and flips its ledger row.

## Current entries

- [file-system-takeover/](file-system-takeover/README.md) — **active, not started.** kcdx takes total ownership of the engine's CCryPak file object (full vtable-pointer swap; kcdx's own PKZIP/DEFLATE reader; a unified load-time asset index; a per-slot reversible thunk table) — every file call dispatches into kcdx, every handle is operated on kcdx's own CRT, eliminating the cross-CRT crash class (KI-0019/KI-0006) structurally. 4-phase plan (seating spike + cleanup → pak reader + index → real file slots + seam subsumption → verification + closure). Settled design: `docs/design/file-system-takeover.md` (`29a21c5`). Phase tree authored by /plan; canonical phase-grain ledger in [file-system-takeover/README.md](file-system-takeover/README.md).
- [maintainer-tool-fix-divergence-diff/](maintainer-tool-fix-divergence-diff/README.md) — **active, not started.** The s04 `[Fix ▸]` per-field divergence diff (TRD D45): when the maintainer opens the field editor from a `failed` s08 report row, surface WHICH recorded `address_versions` field diverged from the running build — recorded-vs-actual, inline at the field to edit — plus a top "What diverged" summary banner. Re-derived in-browser against the report's divergent DLL; frontend-only (no engine change, no report-schema bump). 3-phase plan (probe divergent-DLL behavior + per-field attribution → the `fixDivergence` pure worker → carry the report's-DLL context + render the inline diff + banner). Settled design: `data/maintainer-tool/design.md` D45 (`6ac2501`) + the s04/s08 screen specs. Phase tree authored by /plan; canonical phase-grain ledger in [maintainer-tool-fix-divergence-diff/README.md](maintainer-tool-fix-divergence-diff/README.md).
- [maintainer-tool-lifecycle-completeness/](maintainer-tool-lifecycle-completeness/README.md) — **active, Phases 1–2 (data-core + backend) DONE; Phase 3 (frontend) not started.** Implement TRD D41 — entity-lifecycle completeness + report-vs-DB reconciliation: a standing needs-action view over the version-relative incomplete-lifecycle set (uncovered-at-V orphan / never-verified / broken-reference), the report worklist reconciling against current DB state (already-acted rows show no-further-action; a close that orphans flags needs-action), the new s09 Needs-action view, and the three s08 Fix-flow gaps. 3-phase plan (data-core: close skip + lifecycle detection → backend: needs-action endpoint + reconciliation classification → frontend: s09 view + s08 reconciliation/Fix-flow). Settled design: `data/maintainer-tool/design.md` D41 (`88bb582`) + the s09 screen spec `data/maintainer-tool/ui/screens/s09-needs-action.md` (`3dcf3e0`). Phase tree authored by /plan; canonical phase-grain ledger in [maintainer-tool-lifecycle-completeness/README.md](maintainer-tool-lifecycle-completeness/README.md).
- [maintainer-tool-audit-trio-identity/](maintainer-tool-audit-trio-identity/README.md) — **COMPLETE (both steps landed + milestone UAT accepted 2026-06-08).** Implemented the settled audit-trio identity + verified_date model (TRD D17a/D17b): `verified_by` is prefilled-but-overrideable and SENT as the confirm `author_name` (the signer becomes the git commit author, closing the D17 FE gap — FE:6bfa833); `verified_date` is a read-only system fact shown only when the row is verified (FE:0cc6d2d). Settled design: `data/maintainer-tool/design.md` §6 US-3 + §10 D17a/D17b (`c99438c`). Canonical phase-grain ledger in [maintainer-tool-audit-trio-identity/README.md](maintainer-tool-audit-trio-identity/README.md).
- [hook-backend-marriage/](hook-backend-marriage/README.md) — **COMPLETE — all 6 phases DONE (function-entry on safetyhook + the mid-hook retirement both live-verified; Phase 4 added foreign-hook coexistence — the prologue classifier + chaining via safetyhook's IP-fixed relocation so kcdx + another mod both fire `game→kcdx→foreign→original`, comp-18/comp-19 fixtures — `1b6500c`/`aca788e`/`847e573`; Phase 5 CLOSED as a design correction — the batch premise was falsified, see below; Phase 6 shipped the backend-layer reference doc `docs/hook-backend.md`). The marriage is delivered.** Marry MinHook + safetyhook behind one `IDetourBackend` interface, each install path routed to its best-fit engine (safetyhook for the bulk — thread-safe install, far-target reach, a vetted mid-hook primitive; MinHook permanent for the loader-lock `early_hook` + bootstrap-pump paths it alone can serve). 6-phase plan (vendor + keystone cap-04 spike → backend seam at `InstallRuntime` (function-entry; dissolves `detour_hook`, retires the redundant `g_installed` map) → retire make_jit_midfunc → foreign-hook detection + chaining (core pillar) → ~~batch install~~ DROPPED → reference doc); the mid-hook retirement was gated on the spike (`ctx.rip`→three-modes). **Phase 5 (batch install) DROPPED on measured evidence:** its premise — safetyhook's `enable()` suspends all threads per hook — was FALSE (`trap_threads` is VEH + `VirtualProtect`, not stop-the-world, `os.windows.cpp:268-318`), so there is no cost to amortize; per-hook `enable()` stays. Independent of Phase 11 (different layers, may run in parallel). Settled design: `docs/design/hook-backend-marriage.md` (`03c934c` — v1 + the §4 re-grounding + the §4.5 batch drop). Phase tree authored by /plan; canonical phase-grain ledger in [hook-backend-marriage/README.md](hook-backend-marriage/README.md).
- [maintainer-tool-verification-engine/](maintainer-tool-verification-engine/README.md) — **active, in progress (Phases 0–5 DONE + ACCEPTED; Phase 6 IN PROGRESS — ALL STEPS LANDED, milestone UI acceptance pending; re-planned 2026-06-10 against D39).** The DLL verification engine (un-deferred R5 + R12). Phase 2 (the browser static checker + per-author UI) + Phase 3 (the C++ engine survival authority, D27) + Phase 4 (the D36 engine rank-ladder + per-kind §11.6 matrix) + **Phase 5 (the `kcdx_verify_all` console-after-save-load sweep PRODUCER: 5.1 v3 schema f07d20c / 5.2 command + save-load precondition 187ad3d / 5.3 producer — per-row stream + D37 incremental JSONL flush + v3 finalize, new src/survival_report.{cpp,h} 9b0ee59)** are COMPLETE + live-accepted (2026-06-09 launches; Phase 5: `ACCEPT-RESULT: PASS kcdx_verify_all` 157/157 rows + cap-95 PASS, the v3 report validates). Follow-up TD-0009 (engine↔browser agreement scoped to 4 identical kinds; 3 superset kinds deferred). A Phase-6 worklist item surfaced by the live sweep: kcdx_id 12 read `failed`/`resolved_va_not_in_live_text` (a real DB-vs-binary divergence the engine correctly surfaced, not a defect). **Phase 6 — ALL STEPS LANDED** (the FE report-ingestion v3 THREE-block worklist + the D32 batch transaction + the D39 data-core re-verify resolve seam): 6.1 s08 worklist (FE:`6e7f3b1`), 6.2 `/confirm/batch` + `confirmBatch` (`42ebd79`+FE:`17bfa12`), 6.2a-fix `valid_through` authored column / D40 (`69f54d2`), 6.2b `reverify_resolver` + `/save/reverify-batch` preview / D39 (`201e646`), 6.3 the two batch actions (FE:`d8771ff`). All gates green (data-core suite 182; FE vitest 478; step-review commit-step each). **NEXT: the milestone UI acceptance checkpoint** — the maintainer experiences the s08 import→three-block-worklist→two-batch-action→s06-confirm flow; on accept, Phase 6 → DONE (the phase gate includes user-facing acceptance). Phase tree authored by /plan; canonical phase-grain ledger in [maintainer-tool-verification-engine/README.md](maintainer-tool-verification-engine/README.md).
- [schema-flatten-survival-fold/](schema-flatten-survival-fold/README.md) — **active, not started.** Fold the `survival` sibling table into `address_versions` and delete it — the settled D22/§11 flat-schema migration so the reference DB is one flat table per concern, one typed column per fact, no polymorphic structure. Safety spine: dual-write → prove equal (157/157) → migrate consumers → delete. 3-phase plan (data-core fold → engine + read consumers → delete + finalize, incl. 156/157 slot/offset into columns + baseline re-capture). Settled design: `data/maintainer-tool/design.md` D22 + §11 (`8c87b2f`). Phase tree authored by /plan; canonical phase-grain ledger in [schema-flatten-survival-fold/README.md](schema-flatten-survival-fold/README.md).
- [maintainer-tool-db-direct/](maintainer-tool-db-direct/README.md) — **active, Phases 1–4 DONE; only Phase 5 (Docker) left.** Build the maintainer tool: DB-direct authoring of the Address Library with CSV auto-export — the COMPLETE six-job **web app** (create entity, re-verify, supersede, deprecate, create version, edit-any-version + compare), built to the settled TRD + React/Mantine UI layer. 5-phase plan (data-core ✅ → FastAPI backend ✅ → React+Mantine frontend spine ✅ → frontend full-jobs ✅ → Docker). The tool is functionally complete + running (used for the verification-engine + audit-trio work this session); KI-0007 + KI-0008 (version-row save bugs) fixed + closed 2026-06-08. Phase tree authored by /plan; canonical phase-grain ledger in [maintainer-tool-db-direct/README.md](maintainer-tool-db-direct/README.md).
- [importer-no-prose-derivation/](importer-no-prose-derivation/README.md) — **active, not started.** Eliminate ALL prose-derivation / regex / inference from the refdata importer — every field the importer needs is its own explicit authored seed CSV column; `notes` is human commentary only. 3-phase plan (audit → columns → author+rewire+delete). Phase tree authored by /plan; canonical phase-grain ledger in [importer-no-prose-derivation/README.md](importer-no-prose-derivation/README.md).
- [db-updator-phase1/](db-updator-phase1/README.md) — **active, not started.** Incremental `apply` mode for `import_to_sqlite.py` — land hand-edited seed-CSV deltas into both reference DBs without a full rebuild, sharing one row-builder with rebuild. Phase 1 of the maintainer-tool flow. Phase tree authored by /plan; canonical phase-grain ledger in [db-updator-phase1/README.md](db-updator-phase1/README.md).
- [statement-resolution-layer/](statement-resolution-layer/README.md) — **COMPLETE (all 3 steps landed; cap-83-stmt-resolve PASS live-confirmed 2026-06-08).** Shipped the curated-function statement subset into `reference.sqlite` (step 1, DB lane — `a9b0e8a`) + the refdb statement-resolution API (step 2, engine lane — the full §9.3 locator catalog + the captures-by-name join, 2a `f26c819` / 2b `abdbee3` / 2c `a60e63b`) + the doc reconcile (step 3, this commit), so the Phase 9.3 `kcdx.locator.*`/`op.*`/`statement.*` surface resolves a locator to a statement at runtime. **Unblocks Phase 9.3 steps 1/2/5.** Phase tree authored by /plan; canonical phase-grain ledger in [statement-resolution-layer/README.md](statement-resolution-layer/README.md).
- [restructure/](restructure/README.md) — **active, in progress.** Manifest-only TOML, Lua-first authoring, owned launcher (kcdx.exe), unified ordered list, kcdx absorbs pak mods. 12-phase plan; Phases 1-10 ship the new author surface; Phase 11 (kcdx owns the one Lua VM via the FIX A shim — design settled 2026-06-05, decomposed into a 7-phase build tree at [`restructure/phase-11-shim-vm/`](restructure/phase-11-shim-vm/README.md), incl. the startup-sequence author contract). Authoritative spec for kcdx v0.2+; supersedes large sections of `docs/design.md`. The folder is the navigable form — one subdir per phase, one document per shippable step, the canonical phase-grain status ledger in [`restructure/README.md`](restructure/README.md); the original monolithic plan is preserved verbatim at [`restructure/00-original-plan.md`](restructure/00-original-plan.md).
- [asset-system/](asset-system/README.md) — **active, Phases 1–2 DONE + acceptance-confirmed; Phase 3 IN PROGRESS (`2b0bd1b`; full Lua + C++ `kcdx.assets.*` parity live — replace-vanilla / add-new / cross-mod / conflict / stock-pak all PASS). NOT complete: a served `.lua` EXECUTING is UNCONFIRMED (design §3) — KI-0006, a heap-corruption bug when a `scripts/mods/<modid>.lua` overlay is keyed (serve mechanism proven; 3 theories falsified; cross-CRT-free confirmed-but-not-trigger), BUNDLED into Phase 11 (FIX A collapses the dual-runtime + reworks serve-execute; user-approved deferral 2026-06-05). The boot-cached runtime SERVE is the separate Phase-11 deferral (KI-0005).** kcdx's full asset system — add, reference, publish, compose, and replace game assets: one `assets/` folder, navigable `kcdx.plugin.<author>.<plugin>.*` cross-plugin references, explicit replacement declarations (sidecar or code), runtime registration/publishing, transparent per-class handling. The most-touched mod-authoring surface for TCs. **Canonical design:** [`../design/asset-replacement.md`](../design/asset-replacement.md) **v2** — the seam is TWO coordinated hooks: HOOK 1 replaces `CCryPak::AdjustFileName` (slot 1, id 152) for the resolution DECISION (reaches replace-vanilla via the pak/mount lane), HOOK 2 returns kcdx's own CRT `FILE*` for the loose OPEN (reaches add-new without the engine's loose-search); both install in the ready-bracket; `sys_pakPriority` neither set nor depended on. 3-phase plan (probe-gated resolution ownership → author surface → regression); the `[entrypoints].assets` overlay map (`2588b33`) is valid landed foundation; manipulation deferred. Supersedes the FOpen-based plan archived at [`../archive/asset-replacement-fopen-plan/`](../archive/asset-replacement-fopen-plan/README.md). Phase tree authored by /plan; canonical phase-grain ledger in [asset-system/README.md](asset-system/README.md).
- [kcdx-cvar-surface/](kcdx-cvar-surface/README.md) — **COMPLETE + live-accepted (`5f42a0a`; all 9 cap-71/cap-72 rows PASS).** Read any game CVar's value by name from Lua + C++: `kcdx.cvar.get_int / get_bool / get_float` + the C++ mirror `kcdxConsoleInterface::GetCVar{Int,Bool,Float}`. Backed by two AP18-approved Address Library entities `ICVar_GetIVal` (vtable[2]/+0x10, i32) + `ICVar_GetFVal` (vtable[4]/+0x20, f32), both verified + gated — provenance `_research/icvar-getival-recon/`. Step 0 (EXTERNAL, maintainer-tool lane): record the 2 entities + fix the id-16 GetCVar stale prose, per `_research/icvar-getival-recon/MAINTAINER-TOOL-HANDOFF.md`; the 5 build steps (engine core → Lua → C++ mirror → cap-71 test → docs) are gated on it. First consumer: the asset-system DirectStorage `KCDX_DSCVAR` live read. Phase tree authored by /plan; canonical phase-grain ledger in [kcdx-cvar-surface/README.md](kcdx-cvar-surface/README.md).
- [fix-a-drop-static-lua.md](fix-a-drop-static-lua.md) — drop static-linked `vendor/lua`, route every `lua_*` through WHGame.dll's symbols. Harvest substantially complete (93/117 LUA_API resolved + ~24 inlined/stripped catalogued, 2026-05-21). The shim build is the restructure's Phase 11, now design-settled + decomposed at [`restructure/phase-11-shim-vm/`](restructure/phase-11-shim-vm/README.md).
- [phase6-listener.md](phase6-listener.md) — CryEngine `IGameFrameworkListener` as second-source-of-truth for save/load
- [hook-api-positional-shorthand.md](hook-api-positional-shorthand.md) — possible shorthand call form for kcdx.hook (`kcdx.hook(target, callback)` for the trivial case); table-of-keys form ships first
- [ide-stubs-from-address-library.md](ide-stubs-from-address-library.md) — build-time generator emits `kcdx-stubs.lua` from `data/seeds/address_names_seed.csv` so editors show hover-docs on `kcdx.addr.<name>`; revisit when shipping public releases
- [smart-replace-conflict-detection.md](smart-replace-conflict-detection.md) — kcdx.hook ships chained hooks for non-replace modes day-one; replace+anything-else falls back to load-order-loses. Smart footprint-based conflict detection lands when real plugins need it.
- [ready-event-and-handle-assert.md](ready-event-and-handle-assert.md) — post-ApplyZone `kcdx.on("ready", ...)` event so a plugin can run code once its hooks are live + assert handle:applied()/reason(). Unblocks the deferred CAP-20 miss-asserts (bad address_id name; conflict-rejection side).
- [seeds-to-tracked-csv-migration/](seeds-to-tracked-csv-migration/README.md) — **COMPLETE (all phases 0–3 + the governance sweep landed; zero residue).** The data layer is fully migrated off the retired `data/seeds/` to D38 — both reference DBs rebuild from the tracked CSV export (curated `data/db-export/` in git + bulk `data/db-export-bulk/` under Git LFS); the ~1.3 GB Ghidra dump retires to expert-only bulk-regeneration; the DB stays out of git (D1 holds). Probe-first (P0 verifies the bulk round-trips CSV-losslessly — the soundness-gate carry-forward) → P1 exporter-over-bulk + LFS + run_rebuild genesis + the widened round-trip oracle → P2 backend/toolchain repoint (+ unblocks the paused verification-engine 6.2 + the pre-existing rebuild-test breakage) → P3 delete the deprecated copy. Core data-layer only; the governance/doc sweep is a deferred follow-up. Settled design: `data/maintainer-tool/design.md` D38 (`f5023f5`). Phase tree authored by /plan; canonical phase-grain ledger in [seeds-to-tracked-csv-migration/README.md](seeds-to-tracked-csv-migration/README.md).
- [before-game-hooks.md](before-game-hooks.md) — author-facing hooks installed at DllMain/LDR-notification timing on a foreign module's export (zone-driven, self-registration via the plugin's own DllMain, first-applied-wins). Design-settled + live-de-risked (PROBE T). **Phase 11** (Lua tier needs FIX A's DllMain VM; C++ tier buildable earlier but bundled by user choice). The BugSplat colon-filename fix is its first consumer. Resolves the zone_gate synthetic-row collision (flip `kcdx.hook` to Either + rework comp-13 at Phase 11).
- [hook-capturing-lambda-context-slot.md](hook-capturing-lambda-context-slot.md) — per-hook `void* userdata` slot on `kcdxHookOptions` so the `Kcdx.h` empowered helpers can accept a capturing lambda. Engine ABI extension (AP11 append-only + JIT thunk branch); the current non-capturing fast path stays as the zero-cost form. Tracked from the 2026-05-28 wrapper-improvements audit (item D); revisit on either user-shipped plugin friction OR an unrelated engine change that adds a per-hook context slot.
- [cpp-console-command-wrapper.md](cpp-console-command-wrapper.md) — empowered `kcdx::console::Command(K, "name", "help", lambda)` wrapper over `kcdxConsoleInterface::RegisterCommand` + typed `kcdx::console::Args` proxy with default-bearing `Get(n, default)`. Lowest-ROI row (#8) of the 2026-05-28 wrapper-improvements audit's surface ranking — deferred from Phase 12's scope because the raw form's ceremony is renamed rather than removed. Revisit on either user-shipped plugin friction at scale OR Phase 12's docs flip leaving `docs/cpp/command.md` as the only common-path lead still on a raw form.
- [declare-scoped-clear.md](declare-scoped-clear.md) — scoped `kcdx.declare.clear_own()` + `kcdx.declare.clear(name)` API (Lua + C++ peers on `kcdxDeclareInterface`) so TC mods can drop their OWN declarations without breaking cross-plugin contracts. The engine-wide `declared_targets::Reset()` stays engine-internal — no public nuke that lets one plugin destroy every other plugin's declarations. Designed during the unified Phase 9.2 H1c test discussion when the test plugin needed Reset and the threat model surfaced; revisit when a TC mod surfaces friction OR a hot-reload flow needs per-plugin teardown.
