# Build outputs
build/
out/
cmake-build-*/
*.asi
!examples/**/*.asi  # keep examples if any
*.dll
!vendor/**/*.dll
!examples/**/*.dll  # example plugin DLLs ship via release zips
*.exe
*.pdb
*.exp
*.lib
*.obj
*.o
*.a

# IDE / tooling
.vs/
.vscode/
*.user
*.suo
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Logs
*.log
kcdx.log

# Local artifacts not for source control
# _download/ — cached downloads (ULAH zip etc.); regenerated by build/release scripts
# release-staging/ — generated drag-drop release; produced by release scripts
# test-install/ — local staging area used when iterating against the live game
_download/
release-staging/
test-install/
KCD2-change-outfit-mod/Tools/WHGame.dll

# Research scratch from outer workspace — predecessor-repo clones, extracted
# sig notes. Lives at workspace root in _research/ but defense-in-depth
# entry here in case anything ever lands inside kcdx/.
_research/