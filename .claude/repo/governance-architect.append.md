## This repo's governance additions

- A governance change to `.claude/rules/anti-patterns.md` or `.claude/anti-pattern-rationale.md` additionally requires the `guard-anti-pattern-consent.ps1` consent gate to have fired (an accept-prompt on those files) before the commit. An AP whose rationale was not blessed is unauthorized.
