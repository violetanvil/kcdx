## Repo additions — senior-architect-consult

- **Design anchor label + path** — `docs/design.md` + `.claude/rules/cornerstones.md` (UX > Capability > Performance; the disassembler test). The take is grounded in these and cites their §sections; the review substance is the architectural-review append the consult also reads.

- **Cornerstone gate (the take + decisions-needed)** — every surfaced option set clears `.claude/rules/cornerstones.md` §"Surfaced options clear the cornerstones first" BEFORE presenting.

- **Sibling-skill names** — the design-dialogue channel is `/design`; `senior-architect-consult` is the focused-question sub-tool. The relay sibling is `senior-architect-reply`.
