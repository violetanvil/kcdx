## Repo additions — senior-architect-reply

- **Design anchor label + path** — `docs/design.md` + `.claude/rules/cornerstones.md` (UX > Capability > Performance; the disassembler test). The audit is grounded in these and cites their §sections in the Findings table's rule column.

- **Sibling-skill name** — the direct-discussion sibling is `senior-architect-consult` (default).
